# configs/models/model_xgb.yaml

model:
  type: xgboost
  n_estimators: 100
  learning_rate: 0.1
  max_depth: 6
  subsample: 1.0
  colsample_bytree: 1.0
  reg_alpha: 0.0         # L1 regularization
  reg_lambda: 1.0        # L2 regularization
  n_jobs: -1
  random_state: 42