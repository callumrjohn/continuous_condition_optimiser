# configs/models/model_rf.yaml

model:
  type: random_forest
  n_estimators: 100
  max_depth: null        # null = no maximum
  min_samples_split: 2
  min_samples_leaf: 1
  n_jobs: -1
  random_state: 42